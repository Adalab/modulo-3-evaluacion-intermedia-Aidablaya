/*const getFilms = () => {
    // Llamamos a la API
    return fetch('https://beta.adalab.es/curso-intensivo-fullstack-recursos/apis/adamdb-v1/books_with_id.json') // Este 5 es el id de Leia Skywalker
      .then((response) => response.json())
      .then((responseData) => {
        // Cuando responde la API podemos limpiar los datos aquí
        return responseData;
        
      });
  };
  
  export default getFilms;*/